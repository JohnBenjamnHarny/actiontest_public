name: PR target test

on:
  pull_request_target:
    types:
      - opened
      - edited

jobs:
  print-number:
    runs-on: ubuntu-latest
    steps:
      - name: Build artefacts
        run: npm run build:cjs && npm run build:bootstrap
